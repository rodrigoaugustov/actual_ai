services:
  actual_server:
    # Build from this checkout so the container runs this fork.
    build:
      context: ../..
      dockerfile: sync-server.Dockerfile
    image: actual-ai-server:local
    ports:
      # This line makes Actual available at port 5006 of the device you run the server on,
      # i.e. http://localhost:5006. You can change the first number to change the port, if you want.
      - '5006:5006'
    # Add configuration variables here when needed. For example:
    # environment:
    #   ACTUAL_HTTPS_KEY: /data/selfhost.key
    #   ACTUAL_HTTPS_CERT: /data/selfhost.crt
    #   ACTUAL_PORT: 5006
    volumes:
      # Change './actual-data' below to the path to the folder you want Actual to store its data in on your server.
      # '/data' is the path Actual will look for its files in by default, so leave that as-is.
      - ./actual-data:/data
    healthcheck:
      # Enable health check for the instance
      test: ['CMD-SHELL', 'node scripts/health-check.js']
      # health check using self signed certs
      # test: ['CMD-SHELL', 'NODE_EXTRA_CA_CERTS=/data/selfhost.crt node scripts/health-check.js']
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
